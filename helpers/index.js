const db_validator = require('./db_validators');
const {message} = require('./message');
const especialidad_medico = require('./especialidad_medico');

module.exports = {
    db_validator,
    message,
    especialidad_medico
}